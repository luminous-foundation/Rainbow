.module string {
    .struct string {
        u16* text
        u64 length
    }

    struct new(u8* text u64 length) {
        VAR struct res

        INST string $res
        MOV $text $res.text
        MOV $length $res.length

        RET $res
    }
}